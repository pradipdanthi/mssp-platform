# Lab validation credentials (KB-011 and post-change checks)
# Copy ONCE to:  /opt/mssp-control/.secrets/validation.env
# Then:           chmod 600 /opt/mssp-control/.secrets/validation.env
# NEVER commit .secrets/validation.env — it is gitignored.
#
# Emails + passwords for kb011 and post-change checks.
# Lab live accounts (DEMO tenants were replaced by Alpha/Beta):

PLATFORM_ADMIN_EMAIL=platform.admin@example.local
SOC_MANAGER_EMAIL=soc.manager@example.local
SOC_ANALYST_EMAIL=soc.analyst@example.local
CUSTOMER_ADMIN_EMAIL=admin@alphawin.com
CUSTOMER_VIEWER_EMAIL=viewer@betawin.com
CUSTOMER_ADMIN_TENANT=ALPHAWINCORP-6VS2
CUSTOMER_VIEWER_TENANT=BETAWINCORP-J3FW

PLATFORM_ADMIN_PASSWORD=
SOC_MANAGER_PASSWORD=
SOC_ANALYST_PASSWORD=
CUSTOMER_ADMIN_PASSWORD=
CUSTOMER_VIEWER_PASSWORD=

# Optional — only if a validator needs admin API proof with a custom user:
# KB065_ADMIN_EMAIL=platform.admin@example.local
# KB065_ADMIN_PASSWORD=
