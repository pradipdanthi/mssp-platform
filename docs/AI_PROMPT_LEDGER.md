# AI Prompt Ledger — MSSP Control Plane

Location: `/opt/mssp-control/docs/AI_PROMPT_LEDGER.md`

This is a running log of significant AI-assisted prompts and changes made to this repository. It exists so anyone (including a future AI agent) can see what was asked, what was actually changed, whether it was validated, and which commit it ended up in.

## How to use this ledger

- Add one row per significant AI-assisted change (a KB module, a bug fix, a meaningful refactor). Small, purely exploratory questions that made no file changes do not need a row.
- Fill in "Commit ID" only after the human has reviewed and committed the change. Use `pending` until then.
- Use the "Validation Result" column to record whether validation passed, failed, or was not yet run — do not leave it blank.
- Keep entries in chronological order (oldest first).

---

## Ledger

| Date | KB Module | Prompt Summary | Files Changed | Validation Result | Commit ID |
|---|---|---|---|---|---|
| 2026-07-13 | KB-009A | Create permanent AI development context/rule files for Cursor, Claude, and future coding agents (AGENTS.md, CLAUDE.md, Cursor rule, KB-009 workflow doc, prompt templates, this ledger). Documentation only, no runtime/code changes. | `AGENTS.md`, `CLAUDE.md`, `.cursor/rules/mssp-control-plane.mdc`, `docs/KB009_AI_DEVELOPMENT_WORKFLOW.md`, `docs/PROMPT_TEMPLATES.md`, `docs/AI_PROMPT_LEDGER.md` | Not yet run (documentation-only change; existing validation commands unaffected — to be confirmed by user) | pending |
| 2026-07-13 | KB-010 (Phase 1) | Implement Authentication/Login + Role-Based Access Control foundation: bcrypt password hashing, JWT access tokens, `POST /auth/login`, `GET /auth/me`, `GET /auth/roles`, `require_roles`/`require_tenant_match` RBAC dependencies, `platform_users.password_hash` migration, role rename `super_admin` → `platform_admin`, demo users seeded (`soc.manager@example.local`, `customer.viewer@demo.local`). Existing `/admin/*`/`/customer/*` preview endpoints intentionally left unprotected (Phase 2 deferred). | `backend-api/app/core/config.py`, `backend-api/app/core/security.py`, `backend-api/app/db/session.py`, `backend-api/app/api/dependencies.py`, `backend-api/app/api/routes/auth.py`, `backend-api/app/schemas/auth.py`, `backend-api/app/services/auth_service.py`, `backend-api/app/main.py`, `backend-api/requirements.txt`, `docker-compose.yml`, `postgres/init/002_kb010_auth_rbac.sql`, `scripts/kb010_create_auth_rbac.sh`, `scripts/kb010_validate_auth_rbac.sh`, `AGENTS.md`, `CLAUDE.md`, `.cursor/rules/mssp-control-plane.mdc`, `docs/KB009_AI_DEVELOPMENT_WORKFLOW.md`, `docs/PROMPT_TEMPLATES.md` | Passed — validated on branch `kb010-auth-rbac`, committed as `7fbb3d2`, tagged `kb010-auth-rbac-phase1-validated` | 7fbb3d2 |
| 2026-07-13 | KB-011 | Protect existing `/admin/*` and `/customer/*` preview endpoints with the KB-010 auth foundation: `Depends(require_roles(*ADMIN_SOC_ROLES))` on all 5 `/admin/*` endpoints, `Depends(get_current_user)` + `require_tenant_match(...)` on both `/customer/*` endpoints (404, not 403, on tenant mismatch — anti-enumeration). Added a permanent second demo tenant (`DEMO2`) and demo accounts for the 3 previously-missing roles (`platform_admin`, `soc_analyst`, `customer_admin`). No changes to `dependencies.py`, `auth.py`, the database schema, or `docker-compose.yml`. | `backend-api/app/main.py`, `scripts/kb011_seed_rbac_fixtures.sh` (new), `scripts/kb011_validate_protected_apis.sh` (new), `docs/KB011_DECISION_QUESTIONS.md` (new), `docs/KB011_IMPLEMENTATION_PLAN.md` (new), `docs/KB011_PROTECTED_APIS_COMPLETION.md` (new), `AGENTS.md`, `CLAUDE.md`, `.cursor/rules/mssp-control-plane.mdc` | **Passed** — user ran `scripts/kb011_seed_rbac_fixtures.sh` then `scripts/kb011_validate_protected_apis.sh` and confirmed result: `KB-011 PROTECTED APIS VALIDATION PASSED`. Committed as `30ef305`, tagged `kb011-protected-apis-validated`. | 30ef305 |
| 2026-07-13 | KB-012 | Backend API Route Modularization Foundation (structure-only, no behavior change): moved the 5 `/admin/*` endpoints into `backend-api/app/api/routes/admin.py`, the 2 `/customer/*` endpoints into `customer.py`, and `GET /`/`GET /health` into `health.py` — all moved unchanged (same paths, methods, SQL, response shapes, `Depends(...)` signatures). `backend-api/app/main.py` reduced to app wiring only (metadata, `FastAPI` object, 4 `include_router` calls). Added `redis_client()` to `backend-api/app/db/session.py`, moved unchanged from `main.py`, consolidating all DB/Redis helpers into one shared module. No changes to `dependencies.py`, `auth.py`, the database schema, or `docker-compose.yml`. | `backend-api/app/main.py`, `backend-api/app/db/session.py`, `backend-api/app/api/routes/health.py` (new), `backend-api/app/api/routes/admin.py` (new), `backend-api/app/api/routes/customer.py` (new), `scripts/kb012_validate_route_modularization.sh` (new), `docs/KB012_ROUTE_MODULARIZATION_COMPLETION.md` (new), `AGENTS.md`, `CLAUDE.md`, `.cursor/rules/mssp-control-plane.mdc` | **Passed** — user ran `scripts/kb012_validate_route_modularization.sh` (which reruns `scripts/kb011_validate_protected_apis.sh` unmodified as its behavior-regression gate) and confirmed result: `KB-012 ROUTE MODULARIZATION VALIDATION PASSED`. Committed as `d2b1663`, tagged `kb012-route-modularization-validated`. | d2b1663 |
| 2026-07-14 | KB-013 | Planning prompt: add an Admin Tenant Management API foundation alongside the existing `GET /admin/tenants` list endpoint — inspect current admin routes and `tenants` schema, propose new endpoints/schemas/RBAC/validation approach, stop for approval before implementing. Approved implementation decisions: Decision 1A — only `platform_admin` can create/update tenants, `soc_manager`/`soc_analyst` stay read-only; Decision 2 — UUID `tenant_id` path parameters (`GET /admin/tenants/{tenant_id}`, `PATCH /admin/tenants/{tenant_id}`). Implemented `GET /admin/tenants/{tenant_id}`, `POST /admin/tenants`, `PATCH /admin/tenants/{tenant_id}` in a new router, added `TenantCreateRequest`/`TenantUpdateRequest`/`TenantDetail` Pydantic schemas, added one `fetch_one_write()` helper to `session.py`, wired into `main.py` with one import + one `include_router` line. No schema, `docker-compose.yml`, or `.env` changes; no `DELETE` endpoint (soft-delete via `status=inactive/suspended` instead); existing `GET /admin/tenants` in `admin.py` untouched. | `backend-api/app/main.py`, `backend-api/app/db/session.py`, `backend-api/app/schemas/tenants.py` (new), `backend-api/app/api/routes/tenant_management.py` (new), `scripts/kb013_validate_admin_tenant_management.sh` (new), `docs/KB013_ADMIN_TENANT_MANAGEMENT_COMPLETION.md` (new), `docs/AI_PROMPT_LEDGER.md` | **Passed** — user ran `scripts/kb013_validate_admin_tenant_management.sh` (which reruns `scripts/kb012_validate_route_modularization.sh`, and through it `scripts/kb011_validate_protected_apis.sh`, unmodified as regression gates) and confirmed result: `KB-013 ADMIN TENANT MANAGEMENT VALIDATION PASSED`. Post-validation checks also confirmed `/health` ok, `/openapi.json` lists the new paths, and the `KBTEST13` validation tenant was fully cleaned up. Committed as `95064ed`, tagged `kb013-admin-tenant-management-validated`. | 95064ed |
| 2026-07-14 | KB-014 | Planning prompt: add an Admin User Management API foundation for `platform_users` (list, get one, create, update, disable/enable, optional password reset) — inspect current admin routes, `platform_users` schema, and existing auth/password-hashing code, propose endpoints/schemas/RBAC/validation approach, stop for approval. Approved decisions A–F: (A) only `platform_admin` can create/update/disable-enable/set-password, `soc_manager`/`soc_analyst` stay read-only; (B) UUID `user_id` path parameters; (C) include an admin-triggered `PATCH /admin/users/{user_id}/password` now (not a self-service forgot-password flow); (D) reuse `PATCH {"status": ...}` for disable/enable, no separate action endpoints; (E) strictly enforce admin/SOC roles must not have `tenant_id`, customer roles must have a valid one; (F) `PATCH /admin/users/{user_id}` limited to `full_name`/`phone`/`status` only — no email/role/tenant/user_type change. Implemented `GET /admin/users`, `GET /admin/users/{user_id}`, `POST /admin/users`, `PATCH /admin/users/{user_id}`, `PATCH /admin/users/{user_id}/password` in a new router; added `UserCreateRequest`/`UserUpdateRequest`/`UserPasswordUpdateRequest`/`UserDetail`/`UsersListResponse` Pydantic schemas with `user_type` always derived server-side from `role`; reused the existing `hash_password()` and `fetch_one_write()` unchanged (no `db/session.py` changes needed). **First validation run failed**: FastAPI's default `RequestValidationError` handler was found to echo the entire submitted request body (including a plaintext `password`) inside a whole-model validator's `422` error `input` field — a real information-disclosure bug, caught by the validation script's own leak-checker. **Fixed** by adding a global, sanitizing `RequestValidationError` handler (`backend-api/app/core/error_handlers.py`, registered in `main.py`) that redacts `input`/`ctx` to `"<redacted>"` whenever a sensitive key (`password`, `new_password`, `password_hash`, `access_token`, `token`, `jwt`, `secret`, `jwt_secret`) is present, without changing the `422` status, `{"detail": [...]}` shape, or any non-sensitive error's content — verified directly with `jq` against sample leak/safe/`loc`-only payloads before rerunning. Also improved the validation script's own leak-checker to use a `jq`-based structural check for real JSON object keys (so it no longer false-positives on `loc: ["body","new_password"]` or the `/admin/users/{id}/password` URL) plus a body-redaction helper so failure diagnostics never print a password or token. No schema, `docker-compose.yml`, or `.env` changes; no `DELETE` endpoint (disable via `status=inactive/locked` instead, preserving `incidents`/`incident_timeline`/`audit_logs` attribution that an `ON DELETE SET NULL` hard-delete would destroy). | `backend-api/app/main.py`, `backend-api/app/schemas/users.py` (new), `backend-api/app/api/routes/user_management.py` (new), `backend-api/app/core/error_handlers.py` (new), `scripts/kb014_validate_admin_user_management.sh` (new), `docs/KB014_ADMIN_USER_MANAGEMENT_COMPLETION.md` (new), `docs/AI_PROMPT_LEDGER.md` | **Passed on second run** — first run failed a leak check in section 12 (password value echoed in a `422` response) due to the FastAPI default-handler issue described above; after the `error_handlers.py` fix and validation-script leak-checker improvement, the user reran `scripts/kb014_validate_admin_user_management.sh` (which reruns `kb013_validate_admin_tenant_management.sh`, and through it `kb012`/`kb011`, unmodified as regression gates) and confirmed result: `KB-014 ADMIN USER MANAGEMENT VALIDATION PASSED`. Post-validation checks also confirmed `/health` ok, `/openapi.json` lists the 3 new paths, and the `kb014.validation.user@example.local` validation user was fully cleaned up. Not yet committed. | pending |
| 2026-07-14 | KB-015 | Planning prompt: add an Admin Appliance Management API foundation — read-only appliance detail, safe appliance metadata/status update, and appliance-activation-token create/list/revoke — inspect current admin routes, `appliances`/`appliance_activation_tokens` schema, and existing token-hashing precedent, propose endpoints/schemas/RBAC/validation approach, stop for approval. Approved decisions A–F: (A) only `platform_admin` can update appliances, create tokens, and revoke tokens, `soc_manager`/`soc_analyst` stay read-only; (B) UUID `appliance_id`/`tenant_id`/`token_id` path parameters, path-param-not-found → `404`; (C) activation-token TTL defaults to 24h, capped at 720h/30 days; (D) full existing `appliances.status` enum is admin-PATCH-able; (E) implement `PATCH .../revoke` now (only from `pending`, else `409`), no manual "expire" action; (F) `PATCH /admin/appliances/{id}` limited to `appliance_name`/`site_name`/`status` only — no agent-reported fields (`agent_version`, `config_version`, `update_status`, `local_ip`, `last_source_ip`, `last_seen_at`, `health_snapshot`, `appliance_uuid`, `git_commit`). Implemented `GET /admin/appliances/{appliance_id}`, `PATCH /admin/appliances/{appliance_id}`, `POST /admin/tenants/{tenant_id}/appliance-activation-tokens`, `GET /admin/tenants/{tenant_id}/appliance-activation-tokens`, `PATCH /admin/appliance-activation-tokens/{token_id}/revoke` in a new, no-shared-prefix router (paths span three URL namespaces); added `ApplianceUpdateRequest`/`ApplianceDetail`/`ActivationTokenCreateRequest`/`ActivationTokenMetadata`/`ActivationTokenCreateResponse`/`ActivationTokensListResponse` Pydantic schemas with `token_hash` and the raw token structurally absent from every model except the one-time creation response; activation tokens generated with `secrets.token_urlsafe(32)`, hashed with `hashlib.sha256` (not bcrypt — a fast, deterministic hash is the correct tool for a 256-bit high-entropy random value, unlike a password); reused the existing `fetch_one_write()` and the KB-014 global validation-error redaction handler unchanged (zero `db/session.py` or `error_handlers.py` changes needed). **First validation run failed**: a validation-*script* bug (not an API bug) — direct-SQL fixture creation captured `psql`'s `INSERT 0 1` command-tag line concatenated onto the returned UUID via a blanket `tr -d '[:space:]'`, producing a corrupted id like `<uuid>INSERT01`; the API correctly rejected it with a clean `422 uuid_parsing`. **Fixed**, in the validation script only, by adding a `UUID_REGEX` constant, a `psql_scalar()` helper (quiet/tuples-only/unaligned `psql` with `ON_ERROR_STOP=1`, trims lines without ever merging them, requires exactly one output line), and a `validate_uuid()` helper that fails immediately on a malformed captured id before any API call is made; also rewrote the fixture-creation queries as `WITH inserted AS (INSERT ... RETURNING ...) SELECT ... FROM inserted;` so `psql` executes a `SELECT` (no command tag to begin with). No application/API code changes were needed for this fix. No schema, `docker-compose.yml`, or `.env` changes; no `DELETE` endpoint for either appliances or activation tokens (soft-delete via `status="retired"`/`status="revoked"` instead, preserving `appliance_heartbeats`/`protected_assets`/`created_by_user_id` history/attribution that a hard delete would destroy or orphan). Appliance self-registration, activation-token redemption, the heartbeat receiver, and the appliance-authentication/credential model were explicitly deferred to KB-016. | `backend-api/app/main.py`, `backend-api/app/schemas/appliances.py` (new), `backend-api/app/api/routes/appliance_management.py` (new), `scripts/kb015_validate_admin_appliance_management.sh` (new), `docs/KB015_ADMIN_APPLIANCE_MANAGEMENT_COMPLETION.md` (new), `docs/AI_PROMPT_LEDGER.md` | **Passed on second run** — first run failed in the validation script's own fake-appliance-fixture-creation step (section 10) due to the `psql` command-tag/whitespace-stripping bug described above (a validation-script bug, confirmed not an API bug: the API's `422` was the correct response to a corrupted input); after the `psql_scalar()`/`validate_uuid()`/CTE fix to `scripts/kb015_validate_admin_appliance_management.sh` only, the user reran it (which reruns `kb014_validate_admin_user_management.sh`, and through it `kb013`/`kb012`/`kb011`, unmodified as regression gates) and confirmed result: `KB-015 ADMIN APPLIANCE MANAGEMENT VALIDATION PASSED`. Post-validation checks also confirmed `/health` ok, `/openapi.json` lists the new paths, and the fake KB-015 validation appliance/activation-token fixtures were fully cleaned up (0 rows). Not yet committed. | pending |
| 2026-07-14 | KB-016 | Planning prompt: add an Appliance Registration and Heartbeat Receiver Foundation — the first appliance-facing (non-JWT, non-admin) API surface, letting an appliance redeem a KB-015 activation token and send periodic heartbeats — inspect current schema/routes, decide whether a schema migration is needed for a durable appliance credential, propose endpoints/schemas/authentication model/validation approach, stop for approval. Approved decisions A–H: (A) every activation-token failure (not found/expired/revoked/used) returns the same generic `401`, never distinguished; (B) `site_name` comes only from the activation token row, not accepted in the request body at all; (C) `appliance_uuid` is optional from the appliance, server-generates `uuid4()` if omitted; (D) a successful heartbeat sets `status="online"` unless current status is `maintenance` (preserved), and a `retired` appliance's heartbeat returns `403`; (E) durable appliance credential stored as 4 new nullable columns directly on `appliances` (`appliance_api_key_hash`, `appliance_api_key_hint`, `appliance_key_created_at`, `appliance_key_last_used_at`) rather than a separate credentials table; (F) router/schema naming `appliance_agent.py`; (G) extend `error_handlers.py`'s `SENSITIVE_KEYS` with `activation_token`/`appliance_api_key`/`api_key`; (H) **deferred** — KB-015's admin appliance response shape was *not* changed to expose any credential indicator (no admin-facing credential visibility in this module). Implemented `POST /appliance/register` (redeem a one-time activation token via `SELECT ... FOR UPDATE` inside one transaction, create the appliance row, then consume the token via a compare-and-swap `UPDATE ... WHERE status='pending'`, appliance created *before* the token is consumed so a duplicate `appliance_name`/`appliance_uuid` never wastes the token, issue a one-time durable appliance API key) and `POST /appliance/heartbeat` (authenticate via `X-Appliance-ID`/`X-Appliance-API-Key` headers — missing headers handled manually as `401`, not FastAPI's default `422` — `hmac.compare_digest` constant-time key comparison, one transaction for the heartbeat insert + appliance update); added `ApplianceRegisterRequest`/`ApplianceRegisterResponse`/`ApplianceHeartbeatRequest`/`ApplianceHeartbeatResponse` Pydantic schemas (`extra="forbid"` so `tenant_id`/`site_name`/`appliance_api_key`/`token_hash`/`appliance_api_key_hash` cannot be smuggled into either request body, `IPvAnyAddress` for `local_ip` so a malformed IP never reaches PostgreSQL as a raw error); added `appliance_auth_service.py` (parallel, appliance-specific counterpart to `auth_service.py`) for `generate_appliance_api_key()`/`hash_secret_sha256()`/`verify_appliance_credentials()`; added one new `db_transaction()` context manager to `db/session.py` (existing `fetch_all`/`fetch_one`/`execute`/`fetch_one_write` unchanged) since registration and heartbeat each need more than one write to succeed or fail together atomically; added a new idempotent migration `postgres/init/003_kb016_appliance_registration_heartbeat.sql` (4 new nullable columns + 1 unique constraint on `appliances`) plus a live-database apply script `scripts/kb016_create_appliance_registration_heartbeat.sh`, following the exact KB-010 migration-script pattern. No schema file was modified, no `docker-compose.yml`/`.env` change, no new dependency (all crypto is Python standard library; `IPvAnyAddress`/`psycopg.types.json.Jsonb` were already available in the pinned `pydantic`/`psycopg[binary]` versions). | `backend-api/app/main.py`, `backend-api/app/db/session.py`, `backend-api/app/core/error_handlers.py`, `backend-api/app/schemas/appliance_agent.py` (new), `backend-api/app/services/appliance_auth_service.py` (new), `backend-api/app/api/routes/appliance_agent.py` (new), `postgres/init/003_kb016_appliance_registration_heartbeat.sql` (new), `scripts/kb016_create_appliance_registration_heartbeat.sh` (new), `scripts/kb016_validate_appliance_registration_heartbeat.sh` (new), `docs/KB016_APPLIANCE_REGISTRATION_HEARTBEAT_COMPLETION.md` (new), `docs/AI_PROMPT_LEDGER.md` | **Passed** — user applied the live-DB migration, rebuilt/restarted `backend-api`, and ran `scripts/kb016_validate_appliance_registration_heartbeat.sh` (which reruns `kb015_validate_admin_appliance_management.sh`, and through it `kb014`/`kb013`/`kb012`/`kb011`, unmodified as regression gates) and confirmed result: `KB-016 APPLIANCE REGISTRATION AND HEARTBEAT VALIDATION PASSED`. Post-validation checks also confirmed `/health` ok, `/openapi.json` lists both new paths, the 4 new columns and the new unique constraint exist, and the fake KB-016 validation appliance/heartbeat/activation-token fixtures were fully cleaned up (0 rows). Not yet committed. | pending |
| 2026-07-14 | KB-017 | Planning prompt: add an Appliance Credential Rotation and Admin Credential Visibility Foundation — safe admin-side visibility into KB-016's durable appliance credential metadata, plus a `platform_admin`-only rotation/reissue capability — inspect KB-015 admin appliance routes/schemas and KB-016 appliance-auth service/heartbeat behavior, confirm no schema migration is needed, evaluate endpoint-placement options (new dedicated endpoints vs. folding into the existing appliance-detail response), propose endpoints/schemas/RBAC/rotation-behavior/validation approach, stop for approval. Approved decisions A–F: (A) extend the existing `appliance_management.py`/`schemas/appliances.py` files only — no new route/schema files, no `main.py` change; (B) rotation resets `appliance_key_last_used_at` to `NULL` (it represents last use of the *current* credential, not the old one); (C) rotation returns HTTP `200`, not `201` (mutates existing credential fields, does not create a new resource); (D) rotation is allowed for `retired` appliances and never changes appliance `status`; (E) a distinct `ADMIN_APPLIANCE_CREDENTIAL_WRITE_ROLES = ("platform_admin",)` constant, not a reuse of the existing `ADMIN_APPLIANCE_WRITE_ROLES`; (F) `POST .../credential/rotate` takes no request body — no reason/note field, no new request schema. Implemented `GET /admin/appliances/{appliance_id}/credential` (safe metadata: `has_appliance_api_key`, `appliance_api_key_hint`, `appliance_key_created_at`, `appliance_key_last_used_at`, `status`, `last_seen_at` — query never selects `appliance_api_key_hash` itself, only an `IS NOT NULL` boolean) and `POST /admin/appliances/{appliance_id}/credential/rotate` (reuses the existing, unchanged `generate_appliance_api_key()` from KB-016's `appliance_auth_service.py`; single `UPDATE ... RETURNING` via the existing `fetch_one_write()`; defensive `UniqueViolation` catch mirroring KB-015's activation-token-hash backstop; returns the new raw `appliance_api_key` exactly once). Both endpoints were added to the already-registered `appliance_management_router`, so `main.py` needed zero changes; rotation needed no `db_transaction()` (a single write statement) and no `error_handlers.py` change (neither endpoint has a new sensitive request-body field name — `GET` takes only the existing `appliance_id` path param, `POST rotate` has no body). Proved the old key stops working and the new key works purely against the existing, unmodified `POST /appliance/heartbeat`, and proved rotation for a `retired` appliance leaves `status` untouched while heartbeat from that appliance still returns `403` even with the freshly rotated key. **Validation-script bug found and fixed** (script only, no application code): the first run failed a false-positive "file unmodified" check that used a content grep for the word `"credential"` against `appliance_agent.py`/`main.py`, both of which legitimately already contained that word from KB-016 without KB-017 having touched either file; replaced with `git diff --quiet --`/`git diff --cached --quiet --` checks against each file, the correct way to prove no working-tree or staged change exists. No database migration was needed — all 4 credential columns already existed from KB-016. | `backend-api/app/api/routes/appliance_management.py`, `backend-api/app/schemas/appliances.py`, `scripts/kb017_validate_appliance_credential_visibility_rotation.sh` (new), `docs/KB017_APPLIANCE_CREDENTIAL_VISIBILITY_ROTATION_COMPLETION.md` (new), `docs/AI_PROMPT_LEDGER.md` | **Passed** — user rebuilt/restarted `backend-api` and ran `scripts/kb017_validate_appliance_credential_visibility_rotation.sh` (which reruns `kb016_validate_appliance_registration_heartbeat.sh`, and through it `kb015`/`kb014`/`kb013`/`kb012`/`kb011`, unmodified as regression gates) and confirmed result: `KB-017 APPLIANCE CREDENTIAL VISIBILITY AND ROTATION VALIDATION PASSED`. Post-validation checks also confirmed `/health` ok, `/openapi.json` lists both new paths, only the expected files changed, Python/Bash syntax passed, Docker services healthy, no fake KB-017 validation data left behind, and backend logs had no `Traceback`/`ERROR`/`CRITICAL` entries. Not yet committed, tagged, or snapshotted. | pending |
| 2026-07-16 | KB-025 | Customer Incident Detail UI: add read-only `GET /customer/incidents/{short_code}/{incident_number}` (auth + `require_tenant_match`, 404 on mismatch), customer-visible timeline (`event_type`/`title`/`created_at` only), related `customer_visible` alerts via `incident_alerts` (KB-022 field set). Omit comments. Frontend: `getCustomerIncidentDetail`, `IncidentDetailPage`, route `/incidents/:incidentNumber`, list links. Validation script creates/cleans temporary DEMO/DEMO2 fixtures. No schema/compose/admin/.env changes. | `backend-api/app/api/routes/customer.py`, `frontend-customer/src/api/customer.ts`, `frontend-customer/src/pages/IncidentDetailPage.tsx` (new), `frontend-customer/src/pages/IncidentsPage.tsx`, `frontend-customer/src/App.tsx`, `scripts/kb025_validate_customer_incident_detail_ui.sh` (new), `docs/KB025_CUSTOMER_INCIDENT_DETAIL_UI.md` (new), `docs/AI_PROMPT_LEDGER.md` | **Passed** — validated, committed, tagged `kb025-incident-detail-validated` | d4eeb7e |
| 2026-07-16 | KB-026 | Customer Recommendations API/UI: add read-only `GET /customer/recommendations/{short_code}` (`customer_visible = true`, all statuses, priority then `created_at DESC`, LIMIT 100). Frontend: `getCustomerRecommendations`, `RecommendationsPage`, route `/recommendations`, Layout nav. Validation creates/cleans DEMO visible/hidden + DEMO2 fixtures. No schema/compose/admin/.env/dashboard write changes. | `backend-api/app/api/routes/customer.py`, `frontend-customer/src/api/customer.ts`, `frontend-customer/src/pages/RecommendationsPage.tsx` (new), `frontend-customer/src/App.tsx`, `frontend-customer/src/components/Layout.tsx`, `scripts/kb026_validate_customer_recommendations_api_ui.sh` (new), `docs/KB026_CUSTOMER_RECOMMENDATIONS_API_UI.md` (new), `docs/AI_PROMPT_LEDGER.md` | **Passed** — validated, committed, tagged `kb026-customer-recommendations-validated` | 4ad25b0 |
| 2026-07-16 | KB-027 | Customer Recommendation Detail UI: add read-only `GET /customer/recommendations/{short_code}/{recommendation_id}` (auth + `require_tenant_match`, `tenant_id` + `id` + `customer_visible = true`, 404 on hidden/wrong tenant). Frontend: `getCustomerRecommendationDetail`, `RecommendationDetailPage`, route `/recommendations/:recommendationId`, list title links. No related alert/incident joins. Validation creates/cleans DEMO/DEMO2 fixtures. | `backend-api/app/api/routes/customer.py`, `frontend-customer/src/api/customer.ts`, `frontend-customer/src/pages/RecommendationDetailPage.tsx` (new), `frontend-customer/src/pages/RecommendationsPage.tsx`, `frontend-customer/src/App.tsx`, `scripts/kb027_validate_customer_recommendation_detail_ui.sh` (new), `docs/KB027_CUSTOMER_RECOMMENDATION_DETAIL_UI.md` (new), `docs/AI_PROMPT_LEDGER.md` | **Passed** — validated, committed, tagged `kb027-customer-recommendation-detail-validated` | 1dccfb7 |
| 2026-07-16 | KB-028 | Customer Dashboard v2 / Portal Polish (frontend-only): compose incidents/alerts/recommendations/assets/reports via `getCustomerDashboardV2` + `Promise.all`; KPI cards; recent lists with detail links; no legacy dashboard API; no backend/schema/compose changes. | `frontend-customer/src/pages/DashboardPage.tsx`, `frontend-customer/src/api/customer.ts`, `frontend-customer/src/styles.css`, `scripts/kb028_validate_customer_dashboard_v2.sh` (new), `docs/KB028_CUSTOMER_DASHBOARD_V2.md` (new), `docs/AI_PROMPT_LEDGER.md` | **Passed** — validated, committed, tagged `kb028-customer-dashboard-v2-validated` | 5823909 |
| 2026-07-16 | KB-029 | Customer Alert Detail UI: add read-only `GET /customer/alerts/{short_code}/{alert_id}` (auth + `require_tenant_match`, `tenant_id` + `id` + `customer_visible = true`, 404 on hidden/wrong tenant). Safe KB-022 field set only; no incident_alerts join. Frontend: `getCustomerAlertDetail`, `AlertDetailPage`, route `/alerts/:alertId`, title links on Alerts + Dashboard. Validation creates/cleans DEMO/DEMO2 security_alerts fixtures. | `backend-api/app/api/routes/customer.py`, `frontend-customer/src/api/customer.ts`, `frontend-customer/src/pages/AlertDetailPage.tsx` (new), `frontend-customer/src/pages/AlertsPage.tsx`, `frontend-customer/src/pages/DashboardPage.tsx`, `frontend-customer/src/App.tsx`, `scripts/kb029_validate_customer_alert_detail_ui.sh` (new), `docs/KB029_CUSTOMER_ALERT_DETAIL_UI.md` (new), `docs/AI_PROMPT_LEDGER.md` | **Passed** — validated, committed, tagged `kb029-customer-alert-detail-validated` | 3633a03 |
| 2026-07-16 | KB-030 | Customer Protected Asset Detail UI: add read-only `GET /customer/assets/{short_code}/{asset_id}` (auth + `require_tenant_match`, `pa.tenant_id` + `pa.id`, 404 on missing/wrong tenant). Safe KB-023 asset field set; LEFT JOIN appliances for `appliance_name`/`site_name` only. No appliance detail. Frontend: `getCustomerAssetDetail`, `AssetDetailPage`, route `/assets/:assetId`, hostname links on Assets page. Validation creates/cleans DEMO/DEMO2 fixtures. | `backend-api/app/api/routes/customer.py`, `frontend-customer/src/api/customer.ts`, `frontend-customer/src/pages/AssetDetailPage.tsx` (new), `frontend-customer/src/pages/AssetsPage.tsx`, `frontend-customer/src/App.tsx`, `scripts/kb030_validate_customer_asset_detail_ui.sh` (new), `docs/KB030_CUSTOMER_ASSET_DETAIL_UI.md` (new), `docs/AI_PROMPT_LEDGER.md` | **Passed** — validated, committed, tagged `kb030-customer-asset-detail-validated` | f85a72b |
| 2026-07-16 | KB-031 | Customer Report Detail UI: add read-only `GET /customer/reports/{short_code}/{report_id}` (auth + `require_tenant_match`, `tenant_id` + `id` + `status IN ('published','archived')`, 404 on draft/wrong tenant). Safe KB-024 field set; derived title; no metrics/file path. Frontend: `getCustomerReportDetail`, `ReportDetailPage`, route `/reports/:reportId`, title links on Reports + Dashboard latest report. Validation creates/cleans DEMO published/draft + DEMO2 fixtures. | `backend-api/app/api/routes/customer.py`, `frontend-customer/src/api/customer.ts`, `frontend-customer/src/pages/ReportDetailPage.tsx` (new), `frontend-customer/src/pages/ReportsPage.tsx`, `frontend-customer/src/pages/DashboardPage.tsx`, `frontend-customer/src/App.tsx`, `scripts/kb031_validate_customer_report_detail_ui.sh` (new), `docs/KB031_CUSTOMER_REPORT_DETAIL_UI.md` (new), `docs/AI_PROMPT_LEDGER.md` | **Passed** — validated, committed `d27bdea`, tagged `kb031-customer-report-detail-validated` | d27bdea |
| 2026-07-16 | KB-032 | AI Context and Documentation Sync: refresh stale agent context after git/tags showed completion through KB-031. Update `AGENTS.md`, `CLAUDE.md`, `.cursor/rules/mssp-control-plane.mdc`, recreate `CONTEXT.md`, correct ledger rows for KB-025–KB-031, add docs/validation for KB-032. No backend/frontend/schema/compose/.env runtime changes. | `AGENTS.md`, `CLAUDE.md`, `.cursor/rules/mssp-control-plane.mdc`, `CONTEXT.md` (new/recreated), `docs/AI_PROMPT_LEDGER.md`, `docs/KB032_AI_CONTEXT_DOC_SYNC.md` (new), `scripts/kb032_validate_ai_context_doc_sync.sh` (new) | **Passed** — validated, committed `8498c7a`, tagged `kb032-ai-context-doc-sync-validated` | 8498c7a |
| 2026-07-16 | KB-033 | Customer Notifications History: add read-only `GET /customer/notifications/{short_code}` (auth + `require_tenant_match`, tenant filter, LIMIT 100). Safe fields only (no recipient PII, provider IDs, error_message, incident/alert IDs). Frontend: `getCustomerNotifications`, `NotificationsPage`, route `/notifications`, Layout nav. No detail/acknowledge/dashboard in this KB. | `backend-api/app/api/routes/customer.py`, `frontend-customer/src/api/customer.ts`, `frontend-customer/src/pages/NotificationsPage.tsx` (new), `frontend-customer/src/App.tsx`, `frontend-customer/src/components/Layout.tsx`, `scripts/kb033_validate_customer_notifications_api_ui.sh` (new), `docs/KB033_CUSTOMER_NOTIFICATIONS_API_UI.md` (new), `docs/AI_PROMPT_LEDGER.md` | **Passed** — validated, committed `3f108f2`, tagged `kb033-customer-notifications-validated` | 3f108f2 |
| 2026-07-16 | KB-034 | Customer Account / Profile Hardening: add `phone` to `UserPublic`; `PATCH /auth/me` for full_name/phone only; `POST /auth/change-password`; Account page profile + password forms. No forgot-password; no email/role/tenant self-edit. | `backend-api/app/api/routes/auth.py`, `backend-api/app/schemas/auth.py`, `backend-api/app/services/auth_service.py`, `backend-api/app/core/error_handlers.py`, `frontend-customer/src/api/auth.ts`, `frontend-customer/src/auth/AuthContext.tsx`, `frontend-customer/src/pages/AccountPage.tsx`, `frontend-customer/src/styles.css`, `scripts/kb034_validate_customer_account_profile_hardening.sh` (new), `docs/KB034_CUSTOMER_ACCOUNT_PROFILE_HARDENING.md` (new), `docs/AI_PROMPT_LEDGER.md` | **Passed** — validated, committed `e9fc82a`, tagged `kb034-customer-account-profile-hardening-validated` | e9fc82a |
| 2026-07-16 | KB-035 | Customer Appliance Detail UI: add read-only `GET /customer/appliances/{short_code}/{appliance_id}` (auth + `require_tenant_match`, tenant + id filter, 404 on mismatch). Safe KB-023 posture fields plus `config_version`, `update_status`, `latest_heartbeat_at`, linked protected assets. Add `appliance_id` to assets list appliances. Frontend: `getCustomerApplianceDetail`, `ApplianceDetailPage`, route `/appliances/:applianceId`, appliance name links on Assets page. | `backend-api/app/api/routes/customer.py`, `frontend-customer/src/api/customer.ts`, `frontend-customer/src/pages/ApplianceDetailPage.tsx` (new), `frontend-customer/src/pages/AssetsPage.tsx`, `frontend-customer/src/App.tsx`, `scripts/kb035_validate_customer_appliance_detail_ui.sh` (new), `docs/KB035_CUSTOMER_APPLIANCE_DETAIL_UI.md` (new), `docs/AI_PROMPT_LEDGER.md` | **Passed** — validated, committed `1ac1df3`, tagged `kb035-customer-appliance-detail-validated` | 1ac1df3 |
| 2026-07-16 | KB-036 | MSSP Platform Architecture and Deployment Model Roadmap (enterprise revision): document full MSSP/SOC/MDR/XDR vision, cloud/on-prem/hybrid deployment models, enterprise capability stack (Wazuh, Suricata, Zeek, TheHive, Shuffle, MISP, Greenbone, Velociraptor, Ansible/Compose, Prometheus/Grafana), VM 100–111 layout, normalization record types, customer safety boundaries, KB-037–060 roadmap. Sync CONTEXT/AGENTS/CLAUDE/Cursor rules. Docs only — no runtime changes. | `CONTEXT.md`, `AGENTS.md`, `CLAUDE.md`, `.cursor/rules/mssp-control-plane.mdc`, `docs/KB036_MSSP_PLATFORM_ARCHITECTURE_ROADMAP.md` (new/rewritten), `scripts/kb036_validate_mssp_platform_architecture_roadmap.sh` (new/rewritten), `docs/AI_PROMPT_LEDGER.md` | **Passed** — validated, committed `c39fddc`, tagged `kb036-mssp-platform-architecture-roadmap-validated` | c39fddc |
| 2026-07-16 | KB-037 | Cluster and Appliance Registry Planning: design `soc_clusters` registry, tenant `primary_cluster_id` mapping, appliance registry extensions (`deployment_role`, `cluster_id`, `source_platform`, `sync_health_status`), capacity model (agents/EPS/storage/retention), future admin API sketch, security boundaries. Defer tenant `deployment_mode` to KB-038. Docs only — no schema/runtime changes. | `docs/KB037_CLUSTER_APPLIANCE_REGISTRY_PLANNING.md` (new), `scripts/kb037_validate_cluster_appliance_registry_planning.sh` (new), `CONTEXT.md`, `docs/AI_PROMPT_LEDGER.md` | **Passed** — validated, committed `688f3cc`, tagged `kb037-cluster-appliance-registry-planning-validated` | 688f3cc |
| 2026-07-17 | KB-038 | Tenant Deployment Mode Model Planning: define `deployment_mode` enum (`cloud`, `on_prem`, `hybrid`), routing rules per mode, cluster/appliance requirements, hybrid combined model, admin onboarding workflow sketch, customer visibility (safe label only, no raw logs/cluster internals). Links to KB-037. Docs only — no schema/runtime changes. | `docs/KB038_TENANT_DEPLOYMENT_MODE_PLANNING.md` (new), `scripts/kb038_validate_tenant_deployment_mode_planning.sh` (new), `CONTEXT.md`, `docs/AI_PROMPT_LEDGER.md` | **Passed** — committed `39960c0`, tagged `kb038-tenant-deployment-mode-planning-validated` | 39960c0 |
| 2026-07-17 | KB-039–KB-055, KB-059–KB-060 | Overnight autonomous batch (user-approved): deployment automation Ansible foundation; Wazuh/Suricata/Zeek/TheHive/Shuffle/MISP/Greenbone/Velociraptor planning docs + stub playbooks; multi-cluster capacity plan; backup/monitoring/ops runbook. No live SOC installs, no schema. | `ansible/` (new), `docs/KB039_*.md` … `docs/KB055_*.md`, `docs/KB059_*.md`, `docs/KB060_*.md`, matching `scripts/kb0NN_validate_*.sh` | **Passed** — each docs validator PASS; commits on `kb039-kb060-platform-roadmap-execution` | see git log |
| 2026-07-17 | KB-041 execution preparation | Replace the Wazuh debug stub with safe-default, version-pinned infrastructure automation for VM 101. Add explicit preflight/install/validate modes, VM identity guard, separate live-approval flag, installer SHA-256 gate, package/service/listener validation, repository disablement, rollback documentation, and secret-safe execution. | `ansible/playbooks/wazuh-stack-install.yml`, `ansible/group_vars/all.yml`, `ansible/README.md`, `ansible/roles/wazuh_stack/` (new), `docs/KB041_WAZUH_STACK_INSTALLATION_VALIDATION.md`, `scripts/kb041_validate_wazuh_stack_installation_validation.sh`, `CONTEXT.md`, `docs/AI_PROMPT_LEDGER.md` | **Passed locally** — YAML, safety interlocks, secret scan, and `ansible-playbook --syntax-check` passed; no inventory hosts contacted | 27bebec |
| 2026-07-17 | KB-041 lab infrastructure preparation | With explicit per-step approval, create VM 101 `wazuh-stack` (`192.168.0.211`, Ubuntu 24.04.4, 4 vCPU/16 GB/200 GB) and dedicated VM 112 `automation` (`192.168.0.222`, Ubuntu 24.04.4, 2 vCPU/4 GB/40 GB). Update both OSes, install QEMU guest agents, install Ansible Core 2.16.3/Git/rsync on VM 112, establish restricted VM 100 management keys, take rollback snapshots, and sync the approved automation subset to VM 112. Wazuh remains uninstalled. | Proxmox VMs 101/112; `ansible/inventory/hosts.yml`, `ansible/README.md`, `docs/KB039_DEPLOYMENT_AUTOMATION_FOUNDATION.md`, `docs/KB041_WAZUH_STACK_INSTALLATION_VALIDATION.md`, `CONTEXT.md`, `docs/AI_PROMPT_LEDGER.md`; `/home/secadmin/mssp-automation/` on VM 112 | **Passed** — snapshots `kb041-os-updated` and `kb112-ansible-ready`; local/remote content SHA-256 matched; inventory and playbook syntax passed on VM 112; no playbook targeted VM 101 | d6b8f43 |
| 2026-07-17 | KB-041 Ansible access VM 112→101 | Dedicated Ansible key on VM 112, restricted authorization on VM 101 (`from=192.168.0.222`), passwordless sudo for Ansible become, inventory SSH settings, ping-only connectivity test. No Wazuh install. | `ansible/inventory/hosts.yml`, `CONTEXT.md`, `docs/AI_PROMPT_LEDGER.md`; VM 112 key `~/.ssh/id_ed25519_ansible_wazuh`; VM 101 `/etc/sudoers.d/mssp-ansible` | **Passed** — `ansible wazuh_stack -m ping` → `pong`; install still blocked | f8f0c57 |
| 2026-07-17 | KB-041 live preflight VM 112→101 | Run the Wazuh role against VM 101 in explicit `preflight` mode with Ansible check mode and live-install approval false. Harden read-only checks for CPU/RAM, total/free disk, reboot state, outbound TLS and installer checksum, and required Wazuh TCP ports. No packages or services changed. | `ansible/roles/wazuh_stack/defaults/main.yml`, `ansible/roles/wazuh_stack/tasks/main.yml`, `scripts/kb041_validate_wazuh_stack_installation_validation.sh`, `docs/KB041_WAZUH_STACK_INSTALLATION_VALIDATION.md`, `CONTEXT.md`, `docs/AI_PROMPT_LEDGER.md`; synchronized role files on VM 112 | **Passed** — source validator and syntax check passed; final live recap `ok=15 changed=0 failed=0 skipped=16`; VM resources, reboot state, installer TLS/checksum, ports 443/1514/1515/55000/9200, and rollback snapshots verified; no Wazuh packages or install marker | f8f0c57 |
| 2026-07-17 | KB-041 installer integrity and credential custody | Independently download the official Wazuh 4.14 installation assistant twice without execution, verify matching SHA-256 and official package availability, record the public digest, and enforce root-only custody of the generated credential archive. Prepare—but do not run—the exact live-install command. | `ansible/group_vars/all.yml`, `ansible/roles/wazuh_stack/defaults/main.yml`, `ansible/roles/wazuh_stack/tasks/main.yml`, `scripts/kb041_validate_wazuh_stack_installation_validation.sh`, `docs/KB041_WAZUH_STACK_INSTALLATION_VALIDATION.md`, `CONTEXT.md`, `docs/AI_PROMPT_LEDGER.md` | **Prepared** — two 208288-byte downloads matched SHA-256 `cb7f4ca737a798e4ed98c73579a6105b4dab45aa967bc1c0154f85ab2951b209`; official metadata advertises 4.14.6-1 packages; live installation not executed | f8f0c57 |
| 2026-07-17 | KB-041 live install VM 112→101 | User-approved live all-in-one install of Wazuh 4.14.6 on VM 101 from VM 112, followed by validate mode and package/service/listener confirmation. Credentials left root-only on VM 101; no secrets in Git. | `CONTEXT.md`, `docs/KB041_WAZUH_STACK_INSTALLATION_VALIDATION.md`, `docs/AI_PROMPT_LEDGER.md` | **Passed** — install `ok=24 changed=7 failed=0`; validate `ok=12 changed=0 failed=0`; packages 4.14.6-1 / filebeat 7.10.2-2; services active; ports 443/1514/1515/55000/9200; marker present; creds `600 root:root` | d7d38ec |
| 2026-07-17 | KB-042 Linux agent safe-default automation | Replace Linux agent debug stub with a preflight-gated `wazuh_agent` role targeting only VM 105; keep Windows as a deferred stub; document that Manager 4.14.6 is live and enrollment still requires Vault + separate approval. | `ansible/roles/wazuh_agent/`, `ansible/playbooks/wazuh-agent-linux.yml`, `ansible/playbooks/wazuh-agent-windows.yml`, `docs/KB042_WAZUH_AGENT_ONBOARDING.md`, `scripts/kb042_validate_wazuh_agent_onboarding.sh`, `CONTEXT.md`, `docs/AI_PROMPT_LEDGER.md` | **Passed** — `KB-042 WAZUH AGENT ONBOARDING VALIDATION PASSED` | 6b6c9a1 |
| 2026-07-17 | KB-042 live Linux agent enrollment | Implement real passwordless authd enrollment for VM 105 against live Manager 4.14.6; install pinned wazuh-agent package, start service, validate. No secrets in Git. | `ansible/roles/wazuh_agent/`, `ansible/inventory/hosts.yml`, `scripts/kb042_validate_wazuh_agent_onboarding.sh`, `CONTEXT.md`, `docs/AI_PROMPT_LEDGER.md` | **Passed** — live enroll: agent ID 001 `linux-endpoint-lab` Active on Manager 4.14.6; detection proof: rules 5710 + 5712 (SSH brute force) fired from agent 001 after controlled failed-login burst | 9df18e4 |
| 2026-07-18 | KB-043 Suricata sensor live deploy | Passive IDS on VM 106: safe-default Ansible role, dual-NIC cloud-init VM, isolated `vmbr-capture`, reversible VM105 `tc` mirror, Suricata 7.0.3 install/validate, local ET alert proof. No secrets; Wazuh forward deferred to KB-044. | `ansible/roles/suricata_sensor/`, `ansible/playbooks/suricata-sensor.yml`, `ansible/inventory/hosts.yml`, `docs/KB043_SURICATA_SENSOR_DEPLOYMENT_PLAN.md`, `scripts/kb043_validate_suricata_sensor_deployment_plan.sh`, `CONTEXT.md`, `docs/AI_PROMPT_LEDGER.md` | **Passed** — validator PASS; preflight `ok=13`; install `ok=32 changed=11`; validate `ok=20`; Suricata 7.0.3 active; alert SID 2100498 | e7df02a |
| 2026-07-18 | KB-044 Suricata→Wazuh live integration | Option A: Wazuh agent 4.14.6 on VM 106 tails Suricata `eve.json` to Manager; safe-default `suricata_wazuh` role; end-to-end proof rule 86601. No secrets; customer portal unchanged. | `ansible/roles/suricata_wazuh/`, `ansible/playbooks/suricata-wazuh.yml`, `docs/KB044_SURICATA_WAZUH_INTEGRATION.md`, `scripts/kb044_validate_suricata_wazuh_integration.sh`, `CONTEXT.md`, `docs/AI_PROMPT_LEDGER.md` | **Passed** — validator PASS; agent 002 Active; Manager alerts from `/var/log/suricata/eve.json` rule 86601 | 95be9fd |
| 2026-07-18 | KB-047/048 TheHive+Shuffle co-located | Single VM 102 `thehive_shuffle` 16GB at 192.168.0.212; Docker Compose TheHive4+Cassandra and Shuffle; safe-default case_soar role. Auto-ticket deferred until browser first-login. No secrets in Git. Zeek deferred. | `ansible/roles/case_soar/`, `ansible/playbooks/case-soar.yml`, `ansible/inventory/hosts.yml`, `docs/KB047_THEHIVE_SHUFFLE_COLOCATED_DEPLOY.md`, `scripts/kb047_048_validate_case_soar.sh`, `CONTEXT.md`, `docs/AI_PROMPT_LEDGER.md` | **Passed** — install/validate ok; TheHive :9000 / Shuffle :3001 up | fe662ec |
| 2026-07-17 | KB-056 | Admin/SOC Triage Dashboard Enhancements: alert/incident detail APIs, PATCH triage, comments, list filters, admin detail pages. Backend rebuilt. | `backend-api/app/api/routes/alert_incident_triage.py` (new), `backend-api/app/schemas/triage.py` (new), `backend-api/app/api/routes/admin.py`, `backend-api/app/main.py`, `frontend-admin/...`, `scripts/kb056_validate_*.sh`, `docs/KB056_*.md` | **Passed** — full live triage validation: `KB-056 ADMIN/SOC TRIAGE DASHBOARD ENHANCEMENTS VALIDATION PASSED` | 7554a31 |
| 2026-07-17 | KB-057 | Customer-safe appliance alert ingest: `POST /appliance/alerts`, strict schema, tenant from appliance, duplicate protection, `customer_visible=false` by default. | `backend-api/app/schemas/alert_ingest.py`, `backend-api/app/api/routes/appliance_alert_ingest.py`, `backend-api/app/main.py`, docs/script | **Passed** | 0eecc3a |
| 2026-07-17 | KB-058 | On-prem appliance template + admin download (`GET /admin/appliances/on-prem-template`) + Appliances page button. Placeholders only, no secrets. | `templates/on-prem-appliance/`, `backend-api/app/api/routes/on_prem_template.py`, `frontend-admin/...`, docs/script | **Passed** | ac4dbed |
| 2026-07-22 | KB-049 resume (blocked) | Resume Wazuh→Shuffle→TheHive auto-ticket. Read-only: TheHive `/api/status` OK; Shuffle `/api/v1/health` still `Set up an admin user first!`. Did **not** wire Wazuh. Updated live-status docs + left helper script untracked/ready. | `CONTEXT.md`, `docs/KB049_WAZUH_SHUFFLE_THEHIVE_WORKFLOW.md`, `docs/AI_PROMPT_LEDGER.md`, `scripts/kb049_configure_wazuh_shuffle_integration.sh` (prep) | **Blocked** — superseded by 2026-07-25 live wiring | 8297db6 |
| 2026-07-25 | KB-049 live wiring | Complete Wazuh→Shuffle→TheHive lab auto-ticket: Shuffle/TheHive admin + MSSP-Lab org, webhook workflow, Wazuh shuffle integration level≥10, helper script insert-once fix, proof rule 100049 → TheHive alert confirmed. No secrets in Git. | `docs/KB049_WAZUH_SHUFFLE_THEHIVE_WORKFLOW.md`, `scripts/kb049_configure_wazuh_shuffle_integration.sh`, `docs/KB047_THEHIVE_SHUFFLE_COLOCATED_DEPLOY.md`, `CONTEXT.md`, `docs/AI_PROMPT_LEDGER.md` | **Passed** — live proof + `KB-049 WAZUH SHUFFLE THEHIVE WORKFLOW VALIDATION PASSED` | 8297db6 |

| 2026-07-25 | KB-061 UX + auto-sync | Make Admin/Customer dashboard KPI tiles clickable; harden SOC sync incident backfill for Wazuh-tagged TheHive alerts; add periodic sync runner script. | `frontend-admin/src/pages/DashboardPage.tsx`, `frontend-customer/src/pages/DashboardPage.tsx`, `frontend-*/src/styles.css`, `backend-api/app/services/soc_sync_service.py`, `scripts/kb061_*.sh`, `docs/KB061_*.md`, `CONTEXT.md` | **Passed** — KB-061 validator PASS; TheHive alerts backfilled to INC-DEMO-TH-0003..0005 | pending |

| 2026-07-25 | KB-062 Admin rec/notif + Shuffle hop helper | Add `GET /admin/recommendations` and `GET /admin/notifications`, Admin UI pages/nav/KPI links, Shuffle HTTP hop helper for control-plane sync. | `backend-api/app/api/routes/admin.py`, `frontend-admin/src/pages/RecommendationsPage.tsx`, `frontend-admin/src/pages/NotificationsPage.tsx`, `frontend-admin/src/App.tsx`, `frontend-admin/src/components/Layout.tsx`, `frontend-admin/src/api/admin.ts`, `frontend-admin/src/pages/DashboardPage.tsx`, `docs/KB062_*.md`, `scripts/kb062_*.sh`, `CONTEXT.md`, `docs/AI_PROMPT_LEDGER.md` | **Passed** — `KB-062 ADMIN RECOMMENDATIONS NOTIFICATIONS VALIDATION PASSED` | pending |

| 2026-07-25 | KB-063 instant Wazuh ingress | Dual-path ingress: Wazuh posts to control plane immediately (normalize+incident), forwards to Shuffle/TheHive in background; rewired Manager hook_url; proven with live rule 100049. | `backend-api/app/api/routes/soc_sync.py`, `docker-compose.yml`, `docs/KB063_INSTANT_WAZUH_CONTROL_PLANE_INGRESS.md`, `CONTEXT.md`, `docs/AI_PROMPT_LEDGER.md` | **Passed** — instant ingress HTTP 200 + incident; live Wazuh integrations.log hits new hook | pending |

| 2026-07-25 | KB-064 E2E milestone | Add end-to-end simulation & integration testing milestone (Linux+Windows agents, attack sims, pipeline + dashboard validation) to roadmap/CONTEXT/CLAUDE with readiness gates and proactive prompt duty. | `docs/KB064_E2E_SIMULATION_INTEGRATION_TEST_MILESTONE.md`, `docs/KB036_MSSP_PLATFORM_ARCHITECTURE_ROADMAP.md`, `CONTEXT.md`, `CLAUDE.md`, `docs/AI_PROMPT_LEDGER.md` | **Docs recorded** — full E2E blocked on Windows agent; Phase A Linux rehearsal available now | pending |

---

## Template Row (copy this for new entries)

| Date | KB Module | Prompt Summary | Files Changed | Validation Result | Commit ID |
|---|---|---|---|---|---|
| 2026-07-25 | KB-061 TheHive→control plane sync | Implement normalized `POST /integrations/soc/sync` with X-SOC-Sync-Key, DEMO tenant mapping, alert+high/critical incident create, customer_visible=false; TheHive pull helper; compose secret file mount; live validation PASS; synced MSSP-Lab alerts into DEMO. | `backend-api/app/api/routes/soc_sync.py`, `backend-api/app/schemas/soc_sync.py`, `backend-api/app/services/soc_sync_service.py`, `backend-api/app/main.py`, `backend-api/app/core/error_handlers.py`, `docker-compose.yml`, `.gitignore`, `docs/KB061_THEHIVE_CONTROL_PLANE_SYNC.md`, `scripts/kb061_*.sh`, `CONTEXT.md`, `docs/AI_PROMPT_LEDGER.md` | **Passed** — `KB-061 THEHIVE CONTROL PLANE SYNC VALIDATION PASSED`; TheHive pull ok=3 | pending |
| 2026-07-28 | KB-078 move to VM 109 | User correction: keep control plane free of scanner engines; fix inventory SSH key to `id_ed25519_greenbone`; install Nuclei+Vuls on VM 109 via secadmin+sudo (not root SSH); remove CP install. | `ansible/inventory/hosts.yml`, `ansible/roles/vuln_free_stack/`, `scripts/kb078_*`, `docs/KB078_*`, `CONTEXT.md`, `AGENTS.md`, `CLAUDE.md`, cursor rule, ledger | **Passed** — KB-078 30/30; Nuclei+Vuls on 109 `/opt/mssp-vuln-free`; CP cleaned; NVD fetched; OVAL/gost/KEV fetch continuing | pending |

| 2026-07-28 | KB-078 Nuclei+Vuls free stack | User approved $0 Nuclei+Vuls as primary vuln scanners (better free coverage than Greenbone CE for modern/host CVEs); Enterprise deferred. Ansible role + live install on VM 100; schema source_platform nuclei/vuls; keep Greenbone CE optional. | `docs/KB078_*.md`, `ansible/roles/vuln_free_stack/`, `scripts/kb078_*`, `ansible/inventory/hosts.yml`, `backend-api/app/schemas/vulnerabilities.py`, `CONTEXT.md`, `AGENTS.md`, `CLAUDE.md`, `.cursor/rules/mssp-control-plane.mdc`, `docs/KB077_*.md`, ledger | **Superseded** — moved to VM 109 same day per user | pending |

| 2026-07-28 | KB-077 Greenbone enterprise readiness | User clarified on-prem = production MSSP (not lab); update baseline posture + document Greenbone Community vs Enterprise Feed gaps, Options A/B/C, Phase 1–3 approval gates. Docs only — no Enterprise license/install. | `docs/KB077_GREENBONE_ENTERPRISE_READINESS_PLAN.md`, `scripts/kb077_validate_greenbone_enterprise_readiness_plan.sh`, `CONTEXT.md`, `AGENTS.md`, `CLAUDE.md`, `.cursor/rules/mssp-control-plane.mdc`, `docs/AI_PROMPT_LEDGER.md` | **Passed** — `KB-077` validator 13/13 | pending |
| YYYY-MM-DD | KB-0NN | [One or two sentence summary of what was asked] | `path/to/file1`, `path/to/file2` | [Passed / Failed — reason / Not yet run] | [commit hash or `pending`] |

| 2026-07-25 | KB-065 Admin onboarding UI | Wire Admin Customers/Users pages to existing KB-013/014 APIs (Add/Edit customer, Add/Edit user, set password). No new SQL/schema. Lab=prod path. | `frontend-admin/src/api/admin.ts`, `TenantsPage.tsx`, `UsersPage.tsx`, `Layout.tsx`, `styles.css`, `docs/KB065_*.md`, `scripts/kb065_*.sh`, `.cursor/rules/paused-work-and-ui-gaps.mdc`, `CONTEXT.md`, ledger | **Passed** (UI source + container checks); live API section needs PLATFORM_ADMIN_PASSWORD | pending |
| 2026-07-25 | KB-066 Admin ops catalog | Add Admin Recommendations create/edit, Reports draft/publish, Protected Assets CRUD, Audit log viewer + matching APIs. | `recommendation_management.py`, `admin_ops.py`, schemas, `main.py`, frontend-admin pages/nav/api, `docs/KB066_*.md`, `scripts/kb066_*.sh` | **Passed** — `KB-066 ADMIN OPS CATALOG UI VALIDATION PASSED` | pending |
| 2026-07-25 | KB-067 Enterprise monthly report export | On-screen sections + PDF + Excel downloads; auto snapshot from live DB frozen in metrics; SOC narrative fields; Admin refresh/publish; Customer download for published/archived. | `report_snapshot_service.py`, `report_export_service.py`, `admin_ops.py`, `customer.py`, frontends, `requirements.txt`, `docs/KB067_*.md`, `scripts/kb067_*.sh` | **Passed** — `KB-067 ENTERPRISE MONTHLY REPORT EXPORT VALIDATION PASSED` | pending |
| 2026-07-25 | KB-068 Greenbone Community deploy | Create VM 109 + Ansible role (preflight/install/validate) for official Greenbone Community containers; GSA on https://192.168.0.219; admin password host-local only; no control-plane adapter yet. | `ansible/roles/greenbone/`, `ansible/playbooks/greenbone.yml`, `ansible/inventory/hosts.yml`, `docs/KB068_GREENBONE_COMMUNITY_DEPLOY.md`, `docs/KB052_*.md`, `scripts/kb068_*.sh`, `CONTEXT.md`, `.cursor/rules/paused-work-and-ui-gaps.mdc` | **Passed** — script + ansible validate; HTTPS 200; install ok=32 | pending |
| 2026-07-25 | KB-069 Greenbone control-plane adapter | `vulnerabilities` schema; `POST /integrations/vuln/sync`; Admin list/detail/promote-to-recommendation; Admin UI page; sample ingest; customer portal unchanged. | `postgres/init/004_kb069_*.sql`, `backend-api/.../vuln_*`, `vulnerability_management.py`, `main.py`, `docker-compose.yml`, `frontend-admin/.../Vulnerabilities*`, `docs/KB069_*.md`, `scripts/kb069_*.sh` | **Passed** — `KB-069 GREENBONE CONTROL PLANE ADAPTER VALIDATION PASSED`; sample DEMO ingest ok | pending |
| 2026-07-25 | KB-070 Greenbone live/instant puller | Instant Task-Done HTTP hook on VM 109 → GMP pull (password file mount) → vuln sync; plus on-demand SSH puller + lab scan helper. | `scripts/kb070_*`, `config/greenbone_host_tenant_map.yml`, `docs/KB070_*.md`, `CONTEXT.md`, ledger | **Passed** — `KB-070 GREENBONE LIVE PULLER VALIDATION PASSED`; instant hook `synced=3` | pending |
| 2026-07-26 | Production posture | Convert control plane to production: nginx frontend builds, strip lab/demo dashboard copy, fail-closed Wazuh tenant mapping, THEHIVE_DEFAULT_ORG=`MSSP`, update CONTEXT/AGENTS/Cursor rules. | `frontend-*/Dockerfile`, `nginx.conf`, dashboards/components, `soc_sync.py`, `thehive_client.py`, `docker-compose.yml`, `CONTEXT.md`, `AGENTS.md`, `.cursor/rules/mssp-control-plane.mdc`, `CLAUDE.md`, `docs/KB072_*.md`, ledger | Pending rebuild/restart + UI smoke | pending |
| 2026-07-26 | KB-073 Tenant deployment mode | Add deployment_mode + cloud_provider to tenant onboarding (cloud / on-prem direct / on-prem appliance / hybrid); Admin create/edit + list filter badges. | `postgres/init/008_kb073_*.sql`, `tenant_management.py`, `tenants.py`, `admin.py`, `frontend-admin` TenantsPage/api/styles, `scripts/kb073_*`, `docs/KB073_*.md` | **Passed** — `KB-073 TENANT DEPLOYMENT MODE VALIDATION PASSED` (14/14) | pending |
| 2026-07-26 | KB-073b Cloud + appliance | Split cloud into with/without appliance (`cloud_appliance`); update Admin labels/filters/API/DB check. | `009_kb073b_*.sql`, `tenants.py`, `TenantsPage.tsx`, styles, docs/scripts | **Passed** — KB-073 validator 17/17 | pending |
| 2026-07-29 | KB-088 snapshot | User asked to document + commit all pending work for redeploy readiness: delegated user management, portal-scoped login, SPA `/assets` fix, Windows Sysmon/telemetry bootstrap + installer package, related agent/EDR/purge/migrations. | `docs/KB088_*.md`, `CONTEXT.md`, auth/user-mgmt routes + UIs, vite/nginx, `agent_package_builder.py`, `bootstrap_windows_telemetry.ps1`, deploy/telemetry, validations, ledger | **Partial** — kb088 user-mgmt + windows telemetry packaging validators PASS; live Sysmon on endpoint is operator-run | `e13fc51` |
| 2026-08-01 | Infra gap closure | MISP bridge VM 108 + misp_client; ITDR Graph client (credentials-gated); VR client on 105 + Windows pack; block-hash callback honesty; compose MISP env; honest gap docs. | `misp_client.py`, `itdr_graph_client.py`, `threat_intel_service.py`, `itdr_service.py`, `edr_actions.py`, `docker-compose.yml`, `scripts/kb108_*`, `scripts/kb110_*`, AR scripts, inventory/docs | **Smoke PASS** — health/login/MISP/VR bridge/Linux client | `3e345ba` |
| 2026-08-04 | KB-093 Junexis hardened appliance architecture | Design Junexis-appliance build repo + architecture; refined: no TheHive on appliance, single ISO (physical/VM, on-prem/cloud endpoints), bootstrap Internet then LOCKED SOC-only egress. Docs + scaffold only. | `junexis-appliance/**`, `docs/KB093_*.md`, `scripts/kb093_validate_junexis_appliance_architecture.sh`, `docs/AI_PROMPT_LEDGER.md` | **Passed** — `KB-093 JUNEXIS HARDENED ON-PREM APPLIANCE ARCHITECTURE VALIDATION PASSED` | pending |
| 2026-08-04 | KB-093 B1 build start | Start appliance build: Python junexis-cli (setup/bootstrap/network lock), nft bootstrap+locked profiles, Ansible minimize + runtime install; document Appliance Mgmt separate server for production. | `junexis-appliance/cli/**`, `hardening/nftables/*`, `ansible/roles/{minimize,firewall_nftables,junexis_runtime}/**`, `scripts/kb093b_*`, `docs/KB093_*`, `.cursor/rules/junexis-appliance.mdc`, `CONTEXT.md`, ledger | **Passed** — `KB-093 B1 JUNEXIS CLI BOOTSTRAP-LOCK VALIDATION PASSED` | pending |
| 2026-08-04 | KB-093 B2 Packer disposable VM | Packer/Subiquity Ubuntu 24.04 + Docker/KVM builder image, b2-smoke Ansible (minimize/firewall/runtime), validate gate; full qcow2 optional via JUNEXIS_B2_FULL=1. Appliance Mgmt remains separate-server for production. | `junexis-appliance/packer/**`, `ci/Dockerfile.b2-builder`, `ansible/playbooks/b2-smoke.yml`, `scripts/b2_*`, `scripts/kb093c_*`, `docs/B2_PACKER_*`, KB-093, ledger | **Passed** — `KB-093 B2 PACKER DISPOSABLE VM PIPELINE VALIDATION PASSED` | pending |
| 2026-08-04 | KB-093E appliance engine | DuckDB/ZSTD Parquet datalake, anonymizing telemetry forwarder + SQLite backoff, retrospective hunt API; control-plane `/api/v1/telemetry/*` stub (migrate off VM 100 later). | `junexis-appliance/appliance/**`, `requirements-engine.txt`, `docs/KB093E_*`, `backend-api/.../telemetry_ingest.py`, `main.py`, `scripts/kb093e_*`, ledger | **Passed** — `KB-093E APPLIANCE ENGINE DATALAKE TELEMETRY HUNT VALIDATION PASSED` | pending |
| 2026-08-12 | KB-093P critical-alert forward | Golden recipe + field upgrade: local Manager tails `alerts.json`, forwards level ≥10 to VM 100 ingest; incidents on high/critical; golden VM 199 updated, Beta 210 upgraded. | `kevantic-appliance/**`, `backend-api/.../appliance_alert_*`, `docs/KB093P_*`, `scripts/kb093p_*`, `CONTEXT.md` | **Passed** — KB-093P validator; forwarder active on 199+210 | `ada21fe` |
| 2026-08-12 | Customer entitlement nav | Hide add-on customer tabs and deep links unless tenant entitled. | `frontend-customer/**`, `scripts/kb_validate_customer_entitlement_nav.sh` | **Passed** — entitlement nav validator | `26aed41` |
| 2026-08-12 | Admin dashboard tenant scope | Tenant Scope drives `/admin/dashboard?tenant_id=` and fixes MITRE donut when alert count is zero. | `frontend-admin/**`, `backend-api/app/api/routes/admin.py`, `retrospective_service.py` | Pending redeploy smoke | `539d3bb` |
| 2026-08-12 | Service catalog pricing ops | Admin pricing/rollout + migration 033 + validator. | `service_catalog*.py`, `postgres/init/033_*`, `frontend-admin/ServiceCatalogPage.tsx` | **Passed** — catalog ops validator | `c763893` |
| 2026-08-12 | Kevantic brand consolidation | `packages/brand` canonical source + sync script; portals + website assets. | `packages/brand/**`, `scripts/sync_kevantic_brand.sh`, frontends, `website-junexis/**` | Pending redeploy smoke | `ea5142a` |
