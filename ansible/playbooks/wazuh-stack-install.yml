---
# KB-041: VM 101 Wazuh automation preparation.
# Safe default: preflight only. Live installation requires a separate approval,
# an existing VM 101 snapshot, a verified installer SHA-256, and both explicit
# execution controls documented in the KB-041 runbook.
# No secrets belong in this file or elsewhere in Git.

- name: KB-041 Wazuh stack preflight, install, or validation
  hosts: wazuh_stack
  gather_facts: true
  become: true
  any_errors_fatal: true
  roles:
    - role: wazuh_stack
