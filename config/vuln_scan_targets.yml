# KB-079: Tenant-scoped vulnerability scan targets (Nuclei / Vuls).
# No secrets. Only add targets with written customer scope approval.
#
# Nuclei: IPs, hostnames, or URLs (http/https).
# Vuls: SSH targets (optional) — require scan SSH key on VM 109 only (never Git).

tenants:
  DEMO:
    # Lab linux endpoint (VM 105) decommissioned — add nuclei_targets when manual VM is ready.
    asset_hostname: null
    nuclei_targets: []
    vuls_servers: []
  # Example (disabled until Melvik is entitled and scoped):
  # MELVIK-4B9W:
  #   asset_hostname: melvik-linux-01
  #   nuclei_targets:
  #     - "10.0.0.50"
  #   vuls_servers:
  #     - name: melvik-linux-01
  #       host: "10.0.0.50"
  #       port: "22"
  #       user: scanuser

  ALPHAWINCORP-6VS2:
    asset_hostname: WIN-BL72S84GDTF
    nuclei_targets:
      - "http://192.168.0.214"
    vuls_servers: []
