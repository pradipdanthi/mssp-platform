# Default Appliance Management / channel gateway URL baked into lab images.
# Production appliances should use https://soc.junexis.com (or your public edge).
# Override at build or runtime with JUNEXIS_DEFAULT_CONTROL_PLANE.
JUNEXIS_DEFAULT_CONTROL_PLANE=http://192.168.0.224:8000
