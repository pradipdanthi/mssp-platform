# Default Appliance Management / channel gateway URL baked into lab images.
# Production appliances should use https://soc.kevantic.com (or your public edge).
# Override at build or runtime with KEVANTIC_DEFAULT_CONTROL_PLANE.
KEVANTIC_DEFAULT_CONTROL_PLANE=http://192.168.0.224:8000
