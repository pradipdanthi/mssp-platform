<agent_config os="windows">
  <!-- MSSP: auto-apply AR scripts + callback env from Manager shared (appliance or cloud). -->
  <wodle name="command">
    <disabled>no</disabled>
    <tag>mssp-edr-ar-sync</tag>
    <interval>1m</interval>
    <run_on_start>yes</run_on_start>
    <timeout>90</timeout>
    <ignore_output>yes</ignore_output>
    <command>powershell.exe -NoProfile -ExecutionPolicy Bypass -Command "$p=@(${env:ProgramFiles(x86)}+'\ossec-agent\shared\Sync-MsspEdrAr.ps1',$env:ProgramFiles+'\ossec-agent\shared\Sync-MsspEdrAr.ps1',$env:ProgramData+'\mssp-edr-ar\Sync-MsspEdrAr.ps1'); foreach($f in $p){ if(Test-Path -LiteralPath $f){ & $f; break } }"</command>
  </wodle>
</agent_config>
